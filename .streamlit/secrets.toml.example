# Streamlit secrets — used by Streamlit Community Cloud (and locally if present).
# Copy to `.streamlit/secrets.toml` for local testing, OR paste these key=value lines
# into the "Secrets" box in Streamlit Cloud → Manage app → Settings → Secrets.

ANTHROPIC_API_KEY = "sk-ant-..."
SEMRUSH_API_KEY = "..."
APOLLO_API_KEY = "..."
