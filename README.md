# seo-tool

Reviews a website for broken links, grammar issues, copywriting improvements, and SEO opportunities. Uses the Anthropic API for analysis and the Semrush API for SEO data.

## Setup

1. Copy `.env.example` to `.env` and fill in your API keys.
2. (Install + run instructions to come once the tool is built.)
